"code": "{{- Coding.code | strip -}}",
{%- assign codeSystem = Coding.codeSystem | get_code_system_uri -%}
"system": "{{- codeSystem -}}",
{%- assign display = Coding.code | get_code_display: codeSystem -%}
{%- if display == nil -%}
  {%- assign display = Coding.displayName | clean_string_from_tabs | strip -%}
{%- endif -%}
"display": "{{- display -}}"