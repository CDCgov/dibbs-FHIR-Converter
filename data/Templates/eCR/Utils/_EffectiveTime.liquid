{% if effectiveTime.low.value or observationEntry.effectiveTime.high.value -%}
    "effectivePeriod": { {% include 'DataType/Period' Period: observationEntry.effectiveTime %} }
{% elsif observationEntry.effectiveTime.value -%}
    "effectiveDateTime":"{{ observationEntry.effectiveTime.value | format_as_date_time }}"
{% endif -%}