{%- assign textVal = text._ | default: text.reference._ -%}
{%- if textVal -%}
    {{- textVal | clean_string_from_tabs | escape_special_chars -}}
{%- elsif text.reference.value -%}
    {%- include 'Utils/TextRefHelper' ref: text.reference.value, origText: origText -%}
{%- endif -%}
